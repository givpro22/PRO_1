<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Togather (To-Gather)</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;700&display=swap" rel="stylesheet">
  <style>
    /* Color and Layout */
    :root {
      --primary-color: #28a745;
      --secondary-color: #ffffff;
    }
    
    body, html { display: flex; flex-direction: column; min-height: 100vh; margin: 0; font-family: 'Poppins', sans-serif; }
    footer { margin-top: auto; background-color: var(--primary-color); color: var(--secondary-color); padding: 1rem; text-align: center; }

    /* Navbar */
    .navbar { background-color: var(--secondary-color); }
    .navbar-brand, .navbar-nav .nav-link { color: var(--primary-color) !important; font-weight: bold; }
    .navbar-brand { font-size: 1.8rem; display: flex; align-items: center; gap: 10px; }

    /* Responsive Navbar */
    .navbar img { height: 30px; }
    @media (min-width: 768px) {
      .navbar img { height: 40px; }
    }

    /* Banner */
    .banner { background-color: var(--primary-color); color: var(--secondary-color); padding: 2rem 0; }
    .banner h2 { font-size: 1.5rem; }
    .banner p { font-size: 1rem; }

    @media (min-width: 768px) {
      .banner h2 { font-size: 2rem; }
      .banner p { font-size: 1.25rem; }
    }

    /* Category Cards */
    .card { border: 0; transition: transform 0.3s ease; }
    .card:hover { transform: scale(1.05); }
    .card-title { color: var(--primary-color); font-weight: bold; }

    /* Equal Card Height */
    .card { height: 100%; }
    .card-body { display: flex; flex-direction: column; justify-content: center; }

    /* Card Spacing on Mobile */
    .row > div { margin-bottom: 1rem; }
  </style>
</head>
<body>

  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg">
    <div class="container d-flex align-items-center">
      <a class="navbar-brand d-flex align-items-center" href="/CNU_report">
        <img src="/images/chonnam_logo.png" height="40" alt="Chonnam Logo" class="img-fluid me-2">
        To-gather
      </a>
      <button class="navbar-toggler ms-auto" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
        <ul class="navbar-nav">
          <% if (loggedIn) { %>
            <li class="nav-item"><a class="nav-link active" href="/logout">logout</a></li>
            <li class="nav-item"><a class="nav-link active" href="/edit">Edit</a></li>                      
            <li class="nav-item"><a class="nav-link active" href="/">
            <%= loggedInUser.username %>
            </a></li>
          <% } else { %>
            <li class="nav-item"><a class="nav-link active" href="/join">Sign In</a></li>
            <li class="nav-item"><a class="nav-link active" href="/login">login</a></li>
            <% } %>
          <li class="nav-item"><a class="nav-link active" href="/">Home</a></li>
          <li class="nav-item"><a class="nav-link" href="/carshare">Restaurants</a></li>
          <li class="nav-item"><a class="nav-link" href="/carshare">Places</a></li>
        </ul>
      </div>
      <img src="/images/club_logo.png" height="40" alt="Club Logo" class="img-fluid ms-2">
    </div>
  </nav>

  <!-- Banner -->
  <div class="container-fluid banner text-center">
    <h2>Explore the best of Chonnam National University!</h2>
    <p class="lead">🍽️🗺️ Discover restaurants and great places around!</p>
  </div>

  <!-- Categories Section -->
  <div class="container my-5">
    <div class="row text-center">
      <div class="col-6 col-md-6">
        <div class="card shadow h-100" onclick="window.location.href='/carshare'">
          <img src="./images/coffee.png" class="card-img-top img-fluid" alt="Restaurants">
          <div class="card-body">
            <h5 class="card-title">Restaurants</h5>
            <p class="card-text">Discover popular dining spots</p>
          </div>
        </div>
      </div>
      <div class="col-6 col-md-6">
        <div class="card shadow h-100" onclick="window.location.href='/carshare'">
          <img src="./images/CarShare.png" class="card-img-top img-fluid" alt="Places">
          <div class="card-body">
            <h5 class="card-title">Places</h5>
            <p class="card-text">Discover must-visit spots</p>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Footer -->
  <footer>&copy; 2024 Togather (To-Gather) - All Rights Reserved</footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
